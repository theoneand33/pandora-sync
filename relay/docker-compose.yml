services:
  relay:
    build: .
    ports: ["8080:8080"]
    volumes: ["relay_data:/data"]
    environment:
      PORT: 8080
      DATA_DIR: /data
      TTL_MINUTES: 30
      MAX_BYTES: 2147483648
    restart: unless-stopped

volumes:
  relay_data:
